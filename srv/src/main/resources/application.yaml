spring:
  config.activate.on-profile: default
  sql.init.schema-locations: classpath:schema-h2.sql
  config:
    activate:
      on-profile: cloud

cds:
  index-page:
    enabled: true
  datasource:
    auto-config.enabled: false

security:
  mock:
    users:
      - name: klaus
        password: pass_klaus
        additional:
          firstName: Klaus
          lastName: Sussard
          email: Klaus.Sussard@email.com

      - name: Mia
        password: pass_mia
        additional:
          firstName: Mia
          lastName: Bonnellac
          email: Mia.Bonnellac@email.com

      - name: Laveena
        password: pass_laveena
        roles:
          - Administrators
        additional:
          firstName: Laveena
          lastName: S
          email: laveena.dayanand-sachdev@capgemini.com